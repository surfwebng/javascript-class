/*
Assignment
1. convert the following functions to IIFE
- sum function from day-02
- area function from day-02
2. refactor sum and area assignment to Es6 (arrow functions)
3. convert the functions below to ES5

```var f3 = (x, y) => {
        var z = x * 2 + y;
        y++;
        x *= 3;
        return (x + y + z) / 2;
};```
*/

var me = (function area(radius){
function area(){
  console.log(area);
}
return Math.PI*(radius * radius);
var area = x.toFixed(2);
}
me.area(2);
